{
  "name": "padme-scan-agent",
  "version": "1.0.0",
  "description": "Agent local de scan (NAPS2 + Tesseract) pour le GED PADME",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "mock": "node src/server.js --mock",
    "dev": "nodemon src/server.js --mock",
    "test": "jest"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.14",
    "supertest": "^7.0.0"
  }
}
